pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,json,jsonc,css,html,md,yml,yaml,toml}"
      run: bun node_modules/oxfmt/bin/oxfmt --check {staged_files}
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bun scripts/lint.ts {staged_files}
